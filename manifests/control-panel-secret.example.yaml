# Copy this file to control-panel-secret.yaml, replace the placeholder, and
# apply it locally. The real Secret file is ignored by git.
apiVersion: v1
kind: Secret
metadata:
  name: ldndrc-control-panel
  namespace: ldndrc
type: Opaque
stringData:
  JWT_SECRET: replace-with-a-long-random-value
